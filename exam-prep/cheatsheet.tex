\documentclass[8pt]{extarticle}
\usepackage[letterpaper,margin=0.5in]{geometry}
\usepackage{multicol}
\usepackage{amsmath,amssymb}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{array}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\small Page \thepage}

\setlength{\parindent}{0pt}
\setlength{\parskip}{4pt}
\setlength{\columnsep}{14pt}
\setlength{\columnseprule}{0.3pt}

\titleformat{\section}{\large\bfseries\color{blue!80!black}}{}{0em}{}
\titleformat{\subsection}{\normalsize\bfseries\color{blue!60!black}}{}{0em}{}
\titlespacing{\section}{0pt}{10pt}{4pt}
\titlespacing{\subsection}{0pt}{8pt}{3pt}

\setlist{leftmargin=14pt, itemsep=2pt, parsep=1pt, topsep=3pt}

\renewcommand{\arraystretch}{1.15}

\begin{document}

\begin{center}
{\LARGE\bfseries STAT GU4204 Midterm Cheat Sheet}
\end{center}

\begin{multicols}{2}

% ============================================================
\section{Key Definitions}

\textbf{Statistical inference}: Procedure producing a probabilistic statement about parts of a statistical model.

\textbf{Parameter}: Characteristic(s) determining the joint distribution of the r.v.'s of interest.

\textbf{Parameter space} $\Omega$: Set of all possible values of $\theta$.

\textbf{Statistic}: $T = \varphi(X_1,\ldots,X_n)$ --- function of data ONLY, no unknown parameters.

\textbf{Estimator}: $\hat\theta_n = \varphi(X_1,\ldots,X_n)$ --- a statistic used to estimate a parameter; it is a \emph{random variable}.

\textbf{Estimate}: $\varphi(x_1,\ldots,x_n)$ --- the realized \emph{number} after plugging in observed data.

\textbf{Bias}: $b(T_n) = E(T_n) - g(\theta)$.

\textbf{Unbiased}: $E(T_n) = g(\theta)$ for ALL $\theta \in \Omega$ (bias $= 0$ everywhere).

\textbf{Consistent}: $\hat\theta_n \xrightarrow{P} \theta$ as $n\to\infty$. Large-sample property; does NOT tell us about finite-$n$ performance.

\textbf{MSE}: $E[(T_n - g(\theta))^2] = \text{Var}(T_n) + [\text{bias}(T_n)]^2$.

\textbf{Dominates}: $T_n$ dominates $S_n$ if $\text{MSE}(T_n) \le \text{MSE}(S_n)$ for ALL $\theta$.

\textbf{Inadmissible}: $S_n$ is inadmissible if some $T_n$ dominates it.

\textbf{MVUE}: Minimum Variance Unbiased Estimator --- unbiased AND has smallest variance among all unbiased estimators.

\textbf{Efficient estimator}: Unbiased estimator that achieves the Cram\'er-Rao lower bound.

\textbf{Sufficient statistic}: $T$ is sufficient for $\theta$ if the conditional distribution of $X|T{=}t$ does NOT depend on $\theta$.

\textbf{Likelihood}: $L(\theta) = \prod f(X_i, \theta)$.

\textbf{Log-likelihood}: $\ell(\theta) = \sum \log f(X_i,\theta)$.

\textbf{Score function}: $\dot\ell(x,\theta) = \frac{\partial}{\partial\theta}\log f(x,\theta)$.

\textbf{Score equation}: $\sum \dot\ell(X_i,\theta) = 0$ (solve for MLE).

\textbf{Fisher information}: $I(\theta) = E[\dot\ell^2(X,\theta)] = \text{Var}[\dot\ell(X,\theta)]$. Score has mean 0.

\textbf{Sampling distribution}: Distribution of a statistic $T = \varphi(X_1,\ldots,X_n)$.

\textbf{Pivot}: $\Psi(\mathbf{X}_n, g(\theta))$ --- function of data AND parameter whose distribution does NOT depend on $\theta$.

\textbf{Confidence interval}: Random interval $(A,B)$ such that $P_\theta(A \le g(\theta) \le B) \ge 1-\alpha$ for all $\theta$. \textbf{Exact} if equality holds.

\textbf{Prior} $\xi(\theta)$: Belief about $\theta$ BEFORE observing data.

\textbf{Posterior} $\xi(\theta|x)$: Updated belief about $\theta$ AFTER observing data.

\textbf{Conjugate prior}: Prior such that posterior belongs to the SAME family as the prior.

\textbf{Bayes estimator}: Minimizes expected posterior loss.
\begin{itemize}
\item Squared error loss $\Rightarrow$ posterior mean
\item Absolute error loss $\Rightarrow$ posterior median
\end{itemize}

% ============================================================
\section{Distributions Reference}

\begin{tabular}{@{}lllll@{}}
\toprule
\textbf{Dist} & \textbf{PDF/PMF} & $E(X)$ & $\text{Var}(X)$ & \textbf{MGF} \\
\midrule
Ber($p$) & $p^x(1{-}p)^{1-x}$ & $p$ & $p(1{-}p)$ & $1{-}p{+}pe^t$ \\[3pt]
Bin($n,p$) & $\binom{n}{k}p^k(1{-}p)^{n-k}$ & $np$ & $np(1{-}p)$ & $(1{-}p{+}pe^t)^n$ \\[3pt]
Poi($\lambda$) & $e^{-\lambda}\lambda^k/k!$ & $\lambda$ & $\lambda$ & $e^{\lambda(e^t-1)}$ \\[3pt]
Exp($\theta$) & $\theta e^{-\theta x}$ & $1/\theta$ & $1/\theta^2$ & $\theta/(\theta{-}t)$ \\[3pt]
$N(\mu,\sigma^2)$ & $\frac{1}{\sqrt{2\pi}\sigma}e^{-\frac{(x-\mu)^2}{2\sigma^2}}$ & $\mu$ & $\sigma^2$ & $e^{\mu t+\sigma^2t^2/2}$ \\[3pt]
Unif$[a,b]$ & $\frac{1}{b-a}$ & $\frac{a+b}{2}$ & $\frac{(b-a)^2}{12}$ & $\frac{e^{tb}-e^{ta}}{t(b-a)}$ \\[3pt]
Gam($\alpha,\beta$) & $\frac{\beta^\alpha}{\Gamma(\alpha)}x^{\alpha-1}e^{-\beta x}$ & $\alpha/\beta$ & $\alpha/\beta^2$ & $\left(\frac{\beta}{\beta-t}\right)^\alpha$ \\[3pt]
$\chi^2_d$ & $= \text{Gam}(d/2, 1/2)$ & $d$ & $2d$ & $(1{-}2t)^{-d/2}$ \\[3pt]
$t_n$ & symm, heavy tails & $0$ & $\frac{n}{n-2}$ & --- \\[3pt]
Beta($\alpha,\beta$) & $\frac{x^{\alpha-1}(1-x)^{\beta-1}}{B(\alpha,\beta)}$ & $\frac{\alpha}{\alpha+\beta}$ & $\frac{\alpha\beta}{(\alpha{+}\beta)^2(\alpha{+}\beta{+}1)}$ & --- \\
\bottomrule
\end{tabular}

\subsection{Gamma Function}

$\Gamma(\alpha) = \int_0^\infty x^{\alpha-1}e^{-x}\,dx$

$\Gamma(\alpha{+}1) = \alpha\,\Gamma(\alpha)$ \qquad $\Gamma(n) = (n{-}1)!$ \qquad $\Gamma(1/2) = \sqrt{\pi}$

\subsection{Reproductive Properties (Indep.\ Sums)}

\begin{tabular}{@{}ll@{}}
\toprule
\textbf{$X_i$ iid from} & $\sum X_i \sim$ \\
\midrule
Ber($p$) & Bin($n, p$) \\
Bin($n_i, p$) & Bin($\sum n_i, p$) \\
Gam($\alpha_i, \beta$) same $\beta$ & Gam($\sum\alpha_i, \beta$) \\
$\chi^2_{m_i}$ & $\chi^2_{\sum m_i}$ \\
Poi($\lambda_i$) & Poi($\sum\lambda_i$) \\
$N(\mu_i, \sigma_i^2)$ & $N(\sum\mu_i, \sum\sigma_i^2)$ \\
\bottomrule
\end{tabular}

\subsection{Normal Properties}

$X \sim N(\mu,\sigma^2) \Rightarrow aX+b \sim N(a\mu+b,\; a^2\sigma^2)$

$\bar X_n \sim N(\mu, \sigma^2/n)$ for iid normal sample.

CDF: $\Phi(z)$, \quad symmetry: $1-\Phi(x) = \Phi(-x)$

Key values: $z_{0.025}=1.96$, \quad $z_{0.05}=1.645$, \quad $z_{0.005}=2.576$

% ============================================================
\section{Probability Inequalities}

\textbf{Markov}: $W \ge 0 \;\Rightarrow\; P(W \ge t) \le E(W)/t$

\textbf{Chebyshev}: $P(|Z - EZ| \ge \tau) \le \text{Var}(Z)/\tau^2$

\textbf{On $\bar X$}: $P(|\bar X - \mu| \ge \tau) \le \sigma^2/(n\tau^2)$

Sample size: want $P(|\bar X - \mu| \ge \varepsilon) \le \delta$, need $n \ge \sigma^2/(\varepsilon^2\delta)$.

% ============================================================
\section{Convergence}

\textbf{In probability} ($Z_n \xrightarrow{P} Z$): $P(|Z_n - Z| > \varepsilon) \to 0$ for all $\varepsilon > 0$.

\textbf{In distribution} ($Z_n \xrightarrow{d} Z$): $F_n(u) \to F(u)$ at all continuity points of $F$.

Conv in prob $\Rightarrow$ conv in dist. Converse is FALSE in general (TRUE if limit is a constant).

\textbf{WLLN}: $X_i$ iid, $E|X_1|<\infty \;\Rightarrow\; \bar X_n \xrightarrow{P} \mu$.

Conditions: iid, finite mean. Proof uses Chebyshev (if variance exists).

\textbf{SLLN}: $P(\lim \bar X_n = \mu) = 1$ \quad (almost sure convergence, STRONGER).

\textbf{CLT}: $X_i$ iid, mean $\mu$, $\sigma^2<\infty$:
$$\frac{\sqrt{n}(\bar X_n - \mu)}{\sigma} \xrightarrow{d} N(0,1)$$

Conditions: iid, finite variance. Gives APPROXIMATE dist for large $n$. Does NOT require normality of $X_i$.

\textbf{Continuous Mapping Theorem}:
\begin{itemize}
\item $Z_n \xrightarrow{P} b$, $g$ cts at $b$ $\Rightarrow$ $g(Z_n) \xrightarrow{P} g(b)$
\item $Z_n \xrightarrow{d} Z$, $g$ continuous $\Rightarrow$ $g(Z_n) \xrightarrow{d} g(Z)$
\end{itemize}

\textbf{Slutsky}: $X_n \xrightarrow{d} X$, $Y_n \xrightarrow{P} c$ (constant) $\Rightarrow$ $X_nY_n \xrightarrow{d} cX$ and $X_n+Y_n \xrightarrow{d} X+c$.

% ============================================================
\section{Delta Method}

If $\sqrt{n}(Z_n - \theta)/\sigma \xrightarrow{d} N(0,1)$ and $g'(\theta) \ne 0$:
$$\sqrt{n}\big(g(Z_n) - g(\theta)\big) \xrightarrow{d} N\!\left(0,\; \sigma^2[g'(\theta)]^2\right)$$

Requires $g'(\theta) \ne 0$. If $g'(\theta)=0$, delta method breaks (need second-order).

Recipe: derivative of $g$, evaluate at $\theta$, square it, multiply by original variance.

% ============================================================
\section{Method of Moments (MOM)}

Set population moments $=$ sample moments, solve:
\begin{enumerate}
\item $\mu_j(\theta) = E(X^j)$ for $j = 1,\ldots,k$ parameters
\item $\hat\mu_j = \frac{1}{n}\sum X_i^j$
\item Solve $\mu_j(\theta) = \hat\mu_j$
\end{enumerate}

\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Dist} & \textbf{MOM Estimator} \\
\midrule
Ber($\theta$), Poi($\lambda$) & $\hat\theta = \bar X$ \\
$N(\mu,\sigma^2)$ & $\hat\mu = \bar X$, \; $\hat\sigma^2 = \frac{1}{n}\sum(X_i-\bar X)^2$ \\
Exp($\lambda$) & $\hat\lambda = 1/\bar X$ \\
Gam($\alpha,\beta$) & use $\bar X = \alpha/\beta$, \; $\hat\mu_2 - \bar X^2 = \alpha/\beta^2$ \\
\bottomrule
\end{tabular}

MOM is consistent (LLN + CMT). Not unique. Not always optimal.

% ============================================================
\section{Maximum Likelihood Estimation (MLE)}

$L(\theta) = \prod f(X_i,\theta)$, \qquad $\ell(\theta) = \sum\log f(X_i,\theta)$

Set $\ell'(\theta) = 0$, verify $\ell''(\hat\theta) < 0$.

\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Dist} & \textbf{MLE} \\
\midrule
Ber($\theta$), Poi($\theta$) & $\hat\theta = \bar X$ \\
$N(\mu,\sigma^2)$ & $\hat\mu = \bar X$, \; $\hat\sigma^2 = \frac{1}{n}\sum(X_i{-}\bar X)^2$ \textbf{(BIASED)} \\
Exp($\theta$) rate & $\hat\theta = 1/\bar X$ \\
Unif$[0,\theta]$ & $\hat\theta = X_{(n)} = \max(X_i)$ (by inspection) \\
Unif$[\theta,\theta{+}1]$ & any $\theta \in [X_{(n)}{-}1,\; X_{(1)}]$ (NOT unique) \\
\bottomrule
\end{tabular}

\textbf{Properties}:
\begin{itemize}
\item \textbf{Invariance}: $g(\hat\theta)$ is MLE of $g(\theta)$ for any function $g$
\item \textbf{Consistent}: $\hat\theta_n \xrightarrow{P} \theta$ (under regularity conditions)
\item \textbf{Caution}: MLE may NOT exist, NOT be unique, NOT be unbiased
\end{itemize}

\textbf{When calculus fails}: Unif$[0,\theta]$: $L(\theta) = \theta^{-n}\,I(\theta \ge X_{(n)})$ --- decreasing in $\theta$, so max at $\hat\theta = X_{(n)}$.

% ============================================================
\section{MSE \& Comparing Estimators}

$$\text{MSE}(T_n) = \text{Var}(T_n) + [\text{bias}(T_n)]^2$$

For unbiased estimator: MSE $=$ Var.

\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Estimator} & \textbf{Unbiased?} & \textbf{MVUE?} & \textbf{Notes} \\
\midrule
$\bar X$ for $\mu$ & Yes (always) & Yes (Normal) & $\text{Var} = \sigma^2/n$ \\[2pt]
$\frac{1}{n}\sum(X_i{-}\bar X)^2$ & No (for $\sigma^2$) & No & $E = \frac{n-1}{n}\sigma^2$ \\[2pt]
$s^2 = \frac{1}{n-1}\sum(X_i{-}\bar X)^2$ & Yes ($\sigma^2$) & Yes (Normal) & Bessel's \\[2pt]
$2\bar X$ for Unif$[0,\theta]$ & Yes & No & MSE $= \theta^2/(3n)$ \\[2pt]
$X_{(n)}$ for Unif$[0,\theta]$ & \textbf{No} & No & MSE $\ll 2\bar X$ \\[2pt]
$(1{+}1/n)X_{(n)}$ & Yes & Yes (MVUE) & MSE $= \frac{\theta^2}{n(n+2)}$ \\
\bottomrule
\end{tabular}

\medskip

Biased can beat unbiased (bias-variance tradeoff).

Unbiased estimators don't always exist: $\theta/(1{-}\theta)$ for Ber($\theta$) has none.

No universally best estimator in general.

% ============================================================
\section{Sufficient Statistics}

$T$ is sufficient for $\theta$ $\;\Leftrightarrow\;$ $f(\mathbf{x},\theta) = u(\mathbf{x}) \cdot v(r(\mathbf{x}), \theta)$ \quad \textbf{(Factorization Criterion)}

\begin{itemize}
\item $u(\mathbf{x})$: depends on data ONLY (not $\theta$)
\item $v(r(\mathbf{x}),\theta)$: $\theta$ enters ONLY through $r(\mathbf{x})$
\end{itemize}

\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Distribution} & \textbf{Sufficient Statistic} \\
\midrule
Poi($\theta$), Ber($\theta$), Exp($\theta$) & $\sum X_i$ \\
$N(\mu,\sigma^2)$ both unknown & $(\sum X_i, \; \sum X_i^2)$ \\
$N(\mu, \sigma^2$ known$)$ & $\sum X_i$ \\
Unif$[0,\theta]$ & $X_{(n)} = \max(X_i)$ \\
Gam($\alpha$ known, $\beta$) & $\sum X_i$ \\
Gam($\alpha$, $\beta$ known) & $\prod X_i$ \\
\bottomrule
\end{tabular}

\subsection{Rao-Blackwell Theorem}

Given ANY estimator $\delta(X)$ and sufficient statistic $T$:
$$\delta_0(T) = E[\delta(X) \mid T]$$

Then: $\text{MSE}(\delta_0) \le \text{MSE}(\delta)$ for all $\theta$ (never worse, often strictly better).

$\Rightarrow$ Always condition on sufficient statistics to improve estimators.

% ============================================================
\section{Bayesian Inference}

$$\text{Posterior} \propto \text{Likelihood} \times \text{Prior}: \quad \xi(\theta|x) \propto f(x|\theta) \cdot \xi(\theta)$$

Bayes est (squared error) $= E[\theta|X]$ (posterior mean).

Bayes est (absolute error) $=$ posterior median.

\subsection{Conjugate Priors}

\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Likelihood} & \textbf{Conjugate Prior} & \textbf{Posterior} \\
\midrule
Ber($\theta$) & Beta($\alpha,\beta$) & Beta($\alpha{+}\sum x_i,\; \beta{+}n{-}\sum x_i$) \\[3pt]
Poi($\lambda$) & Gam($\alpha,\beta$) & Gam($\alpha{+}\sum x_i,\; \beta{+}n$) \\[3pt]
$N(\mu,\sigma^2$ known$)$ & $N(\mu_0, v_0^2)$ & $N(\mu_n, v_n^2)$ \\[3pt]
Exp($\theta$) & Gam($\alpha,\beta$) & Gam($\alpha{+}n,\; \beta{+}\sum x_i$) \\
\bottomrule
\end{tabular}

\subsection{Normal--Normal Conjugate ($\sigma^2$ known)}

Prior: $\mu \sim N(\mu_0, v_0^2)$. \quad Data: $X_1,\ldots,X_n$ iid $N(\mu, \sigma^2)$.

Posterior: $\mu|X \sim N(\mu_n, v_n^2)$ where:
$$\mu_n = \frac{\mu_0/v_0^2 + n\bar X/\sigma^2}{1/v_0^2 + n/\sigma^2}, \qquad v_n^2 = \frac{1}{1/v_0^2 + n/\sigma^2}$$

\begin{itemize}
\item Posterior mean = weighted average of prior mean and $\bar X$, weighted by \textbf{precisions} ($1/\text{variance}$)
\item As $n\to\infty$: posterior concentrates around $\bar X$ (data overwhelms prior)
\item As $v_0^2\to\infty$ (vague prior): posterior $\approx N(\bar X, \sigma^2/n)$
\end{itemize}

% ============================================================
\section{Sampling Distributions}

\subsection{Chi-Squared}

$Z \sim N(0,1) \;\Rightarrow\; Z^2 \sim \chi^2_1$.

$Z_1,\ldots,Z_d$ iid $N(0,1) \;\Rightarrow\; \sum Z_i^2 \sim \chi^2_d$.

$\chi^2_d = \text{Gam}(d/2,\; 1/2)$. \quad $E(\chi^2_d) = d$, \quad $\text{Var}(\chi^2_d) = 2d$.

Reproductive: $\sum \chi^2_{m_i} \sim \chi^2_{\sum m_i}$ (independent).

\subsection{Normal Sampling --- CRITICAL RESULTS}

$X_1,\ldots,X_n$ iid $N(\mu,\sigma^2)$:

\begin{enumerate}
\item $\bar X \sim N(\mu, \;\sigma^2/n)$
\item $s^2 \sim \dfrac{\sigma^2}{n-1}\,\chi^2_{n-1}$, \quad equivalently $\dfrac{S^2}{\sigma^2} = \dfrac{\sum(X_i-\bar X)^2}{\sigma^2} \sim \chi^2_{n-1}$
\item $\bar X$ and $s^2$ are \textbf{INDEPENDENT} (Proposition 6.3)
\end{enumerate}

Independence of $\bar X$ and $s^2$ \textbf{characterizes} normality.

\textbf{Theorem 6.4} (statement only): $Z_i$ iid $N(0,1)$, $A$ orthogonal $\Rightarrow$ $V=AZ$ has $V_i$ iid $N(0,1)$ and $\sum V_i^2 = \sum Z_i^2$.

\subsection{t-Distribution}

$Z \sim N(0,1)$, $V \sim \chi^2_n$, $Z \perp V$:
$$T = \frac{Z}{\sqrt{V/n}} \sim t_n$$

Properties:
\begin{itemize}
\item Symmetric about 0 ($T$ and $-T$ have same distribution)
\item Heavier tails than $N(0,1)$ for finite $n$
\item As $n\to\infty$: $t_n \to N(0,1)$ (by WLLN: $V/n \xrightarrow{P} 1$)
\end{itemize}

\textbf{Theorem 6.5}: $X_i$ iid $N(\mu,\sigma^2)$:
$$\frac{\sqrt{n}(\bar X - \mu)}{s} \sim t_{n-1}$$

% ============================================================
\section{Confidence Intervals}

$(A,B)$ is level $1{-}\alpha$ CI if $P_\theta(A \le g(\theta) \le B) \ge 1{-}\alpha$ for all $\theta$.

\subsection{Method of Pivots}

\begin{enumerate}
\item Construct $\Psi(\text{data}, \theta)$ with known distribution FREE of $\theta$
\item Find quantiles: $P(q_{\beta_1} \le \Psi \le q_{1-\beta_2}) = 1{-}\alpha$ where $\beta_1+\beta_2=\alpha$
\item Invert the inequalities to isolate $g(\theta)$
\end{enumerate}

Equal-tailed ($\beta_1{=}\beta_2{=}\alpha/2$) gives shortest CI for symmetric distributions.

\subsection{Normal Mean CIs}

\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Case} & \textbf{Pivot} & \textbf{Dist} & \textbf{CI} \\
\midrule
$\sigma^2$ known & $\frac{\sqrt{n}(\bar X-\mu)}{\sigma}$ & $N(0,1)$ & $\bar X \pm \frac{\sigma}{\sqrt{n}}z_{\alpha/2}$ \\[6pt]
$\sigma^2$ unknown & $\frac{\sqrt{n}(\bar X-\mu)}{s}$ & $t_{n-1}$ & $\bar X \pm \frac{s}{\sqrt{n}}t_{n-1,\alpha/2}$ \\[6pt]
Large $n$, any dist & $\frac{\sqrt{n}(\bar X-\mu)}{s}$ & $\approx N(0,1)$ & $\bar X \pm \frac{s}{\sqrt{n}}z_{\alpha/2}$ \\
\bottomrule
\end{tabular}

\subsection{Other CIs}

\textbf{Bernoulli} (large $n$): $\;\hat\theta \pm \sqrt{\dfrac{\hat\theta(1-\hat\theta)}{n-1}}\; z_{\alpha/2}$

\textbf{CI for $\sigma^2$} (normal data): Pivot $(n{-}1)s^2/\sigma^2 \sim \chi^2_{n-1}$.
$$\text{CI}: \quad \left(\frac{(n-1)s^2}{\chi^2_{n-1,\alpha/2}},\quad \frac{(n-1)s^2}{\chi^2_{n-1,1-\alpha/2}}\right)$$

\subsection{CI Properties}

\begin{itemize}
\item Width (normal, $\sigma$ known): $2\sigma\, z_{\alpha/2}/\sqrt{n}$
\item Higher confidence (smaller $\alpha$) $\to$ WIDER CI
\item Larger $n$ $\to$ NARROWER CI (width $\propto 1/\sqrt{n}$)
\item $4\times$ sample size $\Rightarrow$ half the CI width
\item $\alpha = 0 \Rightarrow z_{\alpha/2} = \infty \Rightarrow$ infinitely wide
\end{itemize}

\subsection{CI Interpretation (Classic T/F Trap!)}

\textbf{CORRECT}: ``We have \emph{confidence} $\gamma$ that $\theta$ lies in $(a,b)$.''

\textbf{WRONG}: ``$\theta$ lies in $(a,b)$ with \emph{probability} $\gamma$.'' --- $\theta$ is FIXED (not random), the interval is random. After observing data, it either contains $\theta$ or it doesn't.

% ============================================================
\section{Cram\'er-Rao Inequality}

Score: $\dot\ell(x,\theta) = \frac{\partial}{\partial\theta}\log f(x,\theta)$. \quad $E[\dot\ell] = 0$. \quad Fisher info: $I(\theta) = E[\dot\ell^2]$.

For $n$ iid observations: total info $= n\,I(\theta)$.

\subsection{Regularity Conditions (KNOW THESE)}

\textbf{(A.1)} Support $\{x : f(x,\theta) > 0\}$ does NOT depend on $\theta$.

\quad Excludes: Unif$[0,\theta]$, Unif$[\theta,\theta{+}1]$ --- support changes with $\theta$.

\textbf{(A.2)} Can interchange $\frac{\partial}{\partial\theta}$ and $\int$:
$$\frac{\partial}{\partial\theta}\int W(\mathbf{x})\,f(\mathbf{x},\theta)\,d\mathbf{x} = \int W(\mathbf{x})\,\frac{\partial f}{\partial\theta}(\mathbf{x},\theta)\,d\mathbf{x}$$

\textbf{(A.3)} $\frac{\partial}{\partial\theta}\log f(x,\theta)$ exists for all $x \in A$ and all $\theta \in \Omega$, as a finite quantity.

\subsection{The CRLB}

If $T(\mathbf{X}_n)$ is UNBIASED for $g(\theta)$, and A.1--A.3 hold, and $I(\theta) < \infty$:

$$\boxed{\text{Var}_\theta(T) \;\ge\; \frac{[g'(\theta)]^2}{n\,I(\theta)}}$$

Special case $g(\theta) = \theta$: \quad $\text{Var}(T) \ge 1/(n\,I(\theta))$.

Achieves the bound $\Rightarrow$ \textbf{MVUE} (efficient estimator).

\subsection{Common Fisher Information}

\begin{tabular}{@{}lllll@{}}
\toprule
\textbf{Dist} & $f(x,\theta)$ & $\dot\ell(x,\theta)$ & $I(\theta)$ & \textbf{CRLB} \\
\midrule
Ber($\theta$) & $\theta^x(1{-}\theta)^{1-x}$ & $\frac{x-\theta}{\theta(1-\theta)}$ & $\frac{1}{\theta(1-\theta)}$ & $\frac{\theta(1-\theta)}{n}$ \\[4pt]
Poi($\theta$) & $e^{-\theta}\theta^x/x!$ & $x/\theta - 1$ & $1/\theta$ & $\theta/n$ \\[4pt]
$N(\mu,\sigma^2)$ & normal pdf & $(x{-}\mu)/\sigma^2$ & $1/\sigma^2$ & $\sigma^2/n$ \\[4pt]
Exp($\theta$) & $\theta e^{-\theta x}$ & $1/\theta - x$ & $1/\theta^2$ & $\theta^2/n$ \\
\bottomrule
\end{tabular}

\medskip

$\bar X$ is MVUE for: $\mu$ in Normal, $\theta$ in Bernoulli, $\theta$ in Poisson --- all achieve CRLB.

% ============================================================
\section{True/False Traps (by Topic)}

\textbf{Convergence}:
\textbf{F}: conv in dist $\Rightarrow$ conv in prob (only if limit is constant). \;
\textbf{F}: CLT says $\bar X$ is exactly normal (approx only). \;
\textbf{T}: conv in prob $\Rightarrow$ conv in dist.

\textbf{MLE}:
\textbf{F}: MLE always unbiased ($\hat\sigma^2$, $X_{(n)}$ biased). \;
\textbf{F}: MLE always exists/unique. \;
\textbf{T}: MLE invariant: $g(\hat\theta)$ is MLE of $g(\theta)$. \;
\textbf{T}: MLE consistent (reg.\ conds). \;
\textbf{T}: MLE asymp.\ normal: $\hat\theta_n \approx N(\theta, 1/(nI(\theta)))$.

\textbf{Estimators}:
\textbf{F}: unbiased always better (biased $X_{(n)} \ll 2\bar X$ for Unif). \;
\textbf{F}: consistent $\Rightarrow$ unbiased. \;
\textbf{F}: unbiased est always exists ($\theta/(1{-}\theta)$ for Ber: none). \;
\textbf{T}: biased can have lower MSE than any unbiased.

\textbf{Sufficiency \& Info}:
\textbf{T}: 1-to-1 function of suff.\ stat is sufficient. \;
\textbf{T}: suff stat can be vector-valued. \;
\textbf{T}: Rao-Blackwell improves via sufficiency. \;
\textbf{F}: CRLB applies to all dists (fails for Uniform: A.1).

\textbf{CI}: \textbf{F}: ``$\theta \in$ CI with prob $1{-}\alpha$'' ($\theta$ fixed; say confidence).

\textbf{Normal sampling}: \textbf{T}: $\bar X \perp s^2$ (Prop 6.3). \; \textbf{T}: $t_n \to N(0,1)$.

\textbf{Bayesian}: \textbf{T}: posterior $\to$ true $\theta$ as $n\to\infty$ regardless of prior.

\subsection{Fill-in-the-Blank Quick Lookup}

\begin{tabular}{@{}ll@{}}
$Z_n \xrightarrow{P} c$, $g$ cts $\Rightarrow g(Z_n)\xrightarrow{P}g(c)$ & Continuous Mapping Thm \\
$X_n\xrightarrow{d}X$, $Y_n\xrightarrow{P}a$ $\Rightarrow X_nY_n\xrightarrow{d}aX$ & Slutsky's Theorem \\
Minimizes posterior expected loss & Bayes estimator \\
Sq.\ error $\to$ post.\ mean; abs.\ error $\to$ post.\ median & \\
$E[\dot\ell(X,\theta)]$ & $= 0$ (score has mean 0) \\
$I(\theta) = E[\dot\ell^2] = \text{Var}[\dot\ell]$ & Fisher information \\
\end{tabular}

% ============================================================
\section{Computation Recipes}

\begin{enumerate}
\item \textbf{Find MLE}: Write $L(\theta)$, take log, $\ell'(\theta)=0$, check $\ell''<0$. Uniform: by inspection.
\item \textbf{Find MOM}: Set $E(X^j) = \frac{1}{n}\sum X_i^j$, solve for parameters.
\item \textbf{Unbiased?}: Compute $E(\text{est})$, check if $= g(\theta)$ for ALL $\theta$.
\item \textbf{Sufficient stat}: Write joint pdf, factor as $u(\mathbf{x})\cdot v(r(\mathbf{x}),\theta)$.
\item \textbf{Compare estimators}: MSE $=$ Var $+$ Bias$^2$ for each, compare across all $\theta$.
\item \textbf{Show consistency}: Show $\hat\theta_n \xrightarrow{P} \theta$ via LLN, Chebyshev, or CMT.
\item \textbf{Asymptotic dist}: CLT for $\bar X$, then delta method if transformed.
\item \textbf{Construct CI}: Pick pivot ($Z$, $t$, or $\chi^2$), find quantiles, invert.
\item \textbf{Fisher information}: $\dot\ell = \partial/\partial\theta \log f$, then $I(\theta) = E[\dot\ell^2]$.
\item \textbf{Is it MVUE?}: Check unbiased $+$ Var $=$ CRLB, or use Rao-Blackwell.
\item \textbf{Find posterior}: Write $\propto$ likelihood $\times$ prior, identify the distribution kernel.
\item \textbf{Bayes estimator}: Sq.\ error $=$ posterior mean; abs.\ error $=$ posterior median.
\item \textbf{Interpret CI}: Say ``confidence,'' NOT ``probability.'' $\theta$ is fixed.
\item \textbf{Rao-Blackwell}: Find sufficient $T$, compute $E[\delta(X)|T]$.
\item \textbf{Sample size}: Use Chebyshev or CI width formula. $n \ge \sigma^2/(\varepsilon^2\delta)$.
\end{enumerate}

% ============================================================
\section{Workflow Guides (Plug-and-Play)}

\subsection{Quick Classifier}

\begin{tabular}{@{}ll@{}}
``Find $\text{Var}(aX+bY+\cdots)$'' & $\to$ \textbf{W1} \\
``Bound on $P(\ldots)$'' or ``How large $n$?'' & $\to$ \textbf{W2} \\
``Asymptotic dist of $g(\bar X)$'' & $\to$ \textbf{W3} \\
``CDF/dist of $X_{(n)} = \max(X_i)$'' & $\to$ \textbf{W4} \\
``Find the MLE of $\theta$'' & $\to$ \textbf{W5} \\
``Find MLE of $g(\theta)$'' & $\to$ \textbf{W6} \\
``Find MOM'' or ``Show MOM $=$ MLE'' & $\to$ \textbf{W7} \\
``Show $\hat\theta$ is consistent'' & $\to$ \textbf{W8} \\
``Evaluate $\int x^a e^{-bx}dx$'' or ``$E[X^k]$'' & $\to$ \textbf{W9} \\
``Dist of $\sum X_i$'' or ``Find MGF'' & $\to$ \textbf{W10} \\
Fisher info $\to$ efficiency $\to$ asymp MLE $\to$ CI & $\to$ \textbf{W11} \\
``Margin of error $1/k$ as large'' & $\to$ \textbf{W12} \\
``Show $T$ sufficient via conditional dist'' & $\to$ \textbf{W13} \\
\end{tabular}

% ------------------------------------------------------------
\subsection{W1: Variance of Linear Combinations}

\textbf{Trigger}: Find $\text{Var}(a_1X_1 + a_2X_2 + \cdots + c)$

\textbf{Formula}:
$$\text{Var}\!\left(\sum a_iX_i + c\right) = \sum_i a_i^2\text{Var}(X_i) + 2\!\!\sum_{i<j} a_ia_j\text{Cov}(X_i,X_j)$$

\textbf{Steps}: (1) Drop constants. (2) List coefficients $a_i$ \emph{with signs}. (3) Square each $\times$ Var. (4) Cross terms: $2a_ia_j\text{Cov}$. (5) Sum.

\textbf{Check}: Var $\ge 0$. Negative $\Rightarrow$ sign error in cross terms.

% ------------------------------------------------------------
\subsection{W2: Probability Bounds \& Sample Size}

\textbf{Markov} ($W\ge 0$): $P(W\ge t) \le E(W)/t$. Use when only mean known.

\textbf{Chebyshev}: $P(|Z-EZ|\ge \tau) \le \text{Var}(Z)/\tau^2$. Use when variance known.

\textbf{Higher moment}: $P(|X-\mu|\ge t) \le E|X-\mu|^k / t^k$. Use when $\beta_k$ given.

\textbf{Sample size via Chebyshev}: Want $P(|\bar X - \mu|\ge\varepsilon)\le\delta$.
$$\frac{\sigma^2}{n\varepsilon^2}\le\delta \;\Longrightarrow\; \boxed{n \ge \frac{\sigma^2}{\varepsilon^2\delta}}$$

\textbf{Conv in prob via Markov}: Show $E(X_n)\to 0 \;\Rightarrow\; P(X_n\ge\varepsilon)\le E(X_n)/\varepsilon\to 0$.

% ------------------------------------------------------------
\subsection{W3: Delta Method}

\textbf{Trigger}: Asymptotic distribution of $g(Z_n)$ where $\sqrt{n}(Z_n-\theta)/\sigma \xrightarrow{d} N(0,1)$.

\textbf{Standard} ($g'(\theta)\ne 0$):
\begin{enumerate}
\item Identify $Z_n$, its asymptotic mean $\theta$, and asymptotic variance $\sigma^2/n$
\item Compute $g'(\theta)$; verify $g'(\theta)\ne 0$
\item Answer: $g(Z_n) \sim N\!\big(g(\theta),\; (\sigma^2/n)[g'(\theta)]^2\big)$
\end{enumerate}

\textbf{Second-order} ($g'(\theta)=0$): Standard gives degenerate limit.

If $\sqrt{n}\,Z_n \xrightarrow{d} N(0,\sigma^2)$, then $n\,Z_n^2 = (\sqrt{n}\,Z_n)^2 \xrightarrow{d} \sigma^2\chi^2_1$ by CMT.

More generally: $n(g(Z_n)-g(\theta)) \xrightarrow{d} \tfrac{\sigma^2 g''(\theta)}{2}\,\chi^2_1$.

% ------------------------------------------------------------
\subsection{W4: Order Statistics}

\textbf{CDF of max}: $P(X_{(n)}\le y) = [F_X(y)]^n$

\textbf{Convergence}: Let $Z_n = n(X_{(n)}-\theta)$.
$$P(Z_n\le z) = \left[F_X\!\left(\theta + \tfrac{z}{n}\right)\right]^n \;\xrightarrow{n\to\infty}\; e^{z\,f(\theta)}$$
(using $(1+a/n)^n\to e^a$). Rate is $n$, not $\sqrt{n}$.

% ------------------------------------------------------------
\subsection{W5: MLE Computation}

\textbf{Step 0}: Is support $\theta$-dependent? Yes $\to$ inspection. No $\to$ calculus.

\textbf{Calculus path --- step by step}:

\textbf{1.\ Likelihood}: $L(\theta) = \prod f(x_i|\theta)$. Key: $\prod\theta = \theta^n$, \; $\prod e^{-\theta x_i} = e^{-\theta\sum x_i}$.

\textbf{2.\ Log-likelihood}: $\ell(\theta) = \sum\log f(x_i|\theta)$. Drop terms not involving $\theta$.

Key: $\log(\theta^n) = n\log\theta$, \; $\log(e^{-n\theta}) = -n\theta$.

\textbf{3.\ Differentiate}: $\frac{d}{d\theta}[n\log\theta] = n/\theta$, \; $\frac{d}{d\theta}[-n\theta] = -n$, \; $\frac{d}{d\theta}[(\sum x_i)\log\theta] = \sum x_i/\theta$.

\textbf{4.\ Set $\ell'(\theta) = 0$, solve}: Pattern is $n/\theta - (\text{sum}) = 0$.

\textbf{5.\ Verify}: $\ell''(\hat\theta) < 0$ (common: $-n/\theta^2 < 0$ $\checkmark$).

\medskip

\textbf{Exp($\beta$)}: $\ell = n\log\beta - \beta\sum x_i$. $\;\ell' = n/\beta - \sum x_i = 0$. $\;\hat\beta = 1/\bar X$.

\textbf{Ber($\theta$)}: $\ell = (\sum x_i)\log\theta + (n{-}\sum x_i)\log(1{-}\theta)$. $\;\ell' = \sum x_i/\theta - (n{-}\sum x_i)/(1{-}\theta) = 0$. $\;\hat\theta = \bar X$.

\textbf{Power} $f = \theta x^{\theta-1}$: $\ell = n\log\theta + (\theta{-}1)\sum\log x_i$. $\;\ell' = n/\theta + \sum\log x_i = 0$. $\;\hat\theta = -n/\sum\log x_i$.

\medskip

\textbf{Inspection} (Uniform): $L(\theta) = \theta^{-n}I(\theta\ge X_{(n)})$, decreasing $\Rightarrow$ $\hat\theta = X_{(n)}$.

\textbf{Restricted domain}: Find unrestricted MLE. If outside domain, pick closest boundary.

% ------------------------------------------------------------
\subsection{W6: MLE Invariance}

\textbf{Rule}: $\hat\theta$ is MLE of $\theta$ $\;\Rightarrow\;$ $g(\hat\theta)$ is MLE of $g(\theta)$.

Just plug $\hat\theta$ into $g$.

Examples: MLE of median $= ({\log 2})/{\hat\beta}$. \; MLE of $\theta^2 = \hat\theta^2$. \; MLE of $P(X>c) = e^{-c\hat\beta}$.

% ------------------------------------------------------------
\subsection{W7: MOM (\& MOM $=$ MLE)}

\begin{enumerate}
\item Population moments: $\mu_1 = E(X)$, $\mu_2 = E(X^2)$
\item Sample moments: $\hat\mu_1 = \bar X$, $\hat\mu_2 = \frac{1}{n}\sum X_i^2$
\item Set $\mu_k(\theta) = \hat\mu_k$, solve
\end{enumerate}

MOM $=$ MLE for: Ber, Poi, Exp, Normal. NOT for Gamma (MLE needs numerics).

% ------------------------------------------------------------
\subsection{W8: Consistency Proofs}

\textbf{Given}: estimator $\hat\theta_n$ and distribution (so you know $E(X)$). \textbf{Write 3 lines}:

\textbf{LLN + CMT template} (use this 90\% of the time):

Line 1: ``By WLLN, $\bar X_n \xrightarrow{P} E(X) = $ [mean].''

Line 2: ``Let $g(x) = $ [function s.t.\ $g(\bar X) = \hat\theta$]. $g$ is continuous at $x=$ [mean].''

Line 3: ``By CMT, $g(\bar X_n) \xrightarrow{P} g(\text{mean}) = \theta$. $\square$''

\textbf{How to find $g$}: $\hat\theta = 1/\bar X \Rightarrow g(x)=1/x$. \; $\hat\theta = \bar X^2 \Rightarrow g(x)=x^2$.

\medskip

\textbf{Exp}$(\beta)$, $\hat\beta=1/\bar X$: WLLN $\bar X\xrightarrow{P}1/\beta$; $g(x)=1/x$ cts; CMT $\Rightarrow 1/\bar X\xrightarrow{P}\beta$.

\textbf{Any dist}, $\hat\theta=\bar X^2$: WLLN $\bar X\xrightarrow{P}\mu$; $g(x)=x^2$ cts; CMT $\Rightarrow \bar X^2\xrightarrow{P}\mu^2$.

\medskip

\textbf{MSE method} (when they give you $\text{Var}(\hat\theta)$): show bias $\to 0$ and Var $\to 0$, then MSE $\to 0 \Rightarrow \hat\theta_n\xrightarrow{P}\theta$.

% ------------------------------------------------------------
\subsection{W9: Gamma Integrals \& $E[X^k]$}

\textbf{Master formula}: $\displaystyle\int_0^\infty x^{\alpha-1}e^{-\beta x}\,dx = \frac{\Gamma(\alpha)}{\beta^\alpha}$

Match your integral to this form. Read off $\alpha = \text{power}+1$, $\beta = \text{coeff in exponent}$.

\textbf{$E[X^k]$ for $X\sim\text{Gam}(\alpha,\beta)$}:
$$E[X^k] = \frac{\Gamma(\alpha+k)}{\beta^k\,\Gamma(\alpha)} = \frac{\alpha(\alpha{+}1)\cdots(\alpha{+}k{-}1)}{\beta^k}$$

Key: $\Gamma(n)=(n{-}1)!$, \; $\Gamma(1/2)=\sqrt\pi$, \; $\Gamma(\alpha{+}1)=\alpha\,\Gamma(\alpha)$.

% ------------------------------------------------------------
\subsection{W10: MGF Products \& Sampling Dists}

\textbf{Sum of independents}: $M_{\sum X_i}(t) = \prod M_{X_i}(t)$. For iid: $[M_X(t)]^n$.

\textbf{Recognize MGF}:
$(1{-}p{+}pe^t)^n \to \text{Bin}$, \;
$e^{\lambda(e^t-1)} \to \text{Poi}$, \;
$(\beta/(\beta{-}t))^\alpha \to \text{Gam}$

\textbf{Building a $t$-statistic}:
\begin{enumerate}
\item Find $Z\sim N(0,1)$ (normalized sum)
\item Find independent $V\sim\chi^2_m$ (sum of squared normals)
\item $T = Z/\sqrt{V/m} \sim t_m$
\end{enumerate}

\textbf{Key facts}: $Z^2\sim\chi^2_1$. \; $(n{-}1)s^2/\sigma^2\sim\chi^2_{n-1}$. \; $\sqrt{n}(\bar X{-}\mu)/s \sim t_{n-1}$.

% ------------------------------------------------------------
\subsection{W11: Full Estimation Pipeline}

\textbf{Given}: distribution $f(x|\theta)$, sample size $n$. \textbf{$\theta$ and $n$ stay variables.}

\textbf{(a) Fisher info} --- compute score, square it, take expectation:

$\log f \;\to\; \dot\ell = \partial/\partial\theta\log f \;\to\; I_1(\theta) = E[\dot\ell^2] = \text{Var}(\dot\ell)$

$I_n(\theta) = n\,I_1(\theta)$

\begin{tabular}{@{}llll@{}}
\textbf{Dist} & $\dot\ell$ & $I_1(\theta)$ & $I_n(\theta)$ \\
\midrule
Poi($\theta$) & $(x{-}\theta)/\theta$ & $1/\theta$ & $n/\theta$ \\
Exp($\theta$) & $1/\theta - x$ & $1/\theta^2$ & $n/\theta^2$ \\
Ber($\theta$) & $(x{-}\theta)/[\theta(1{-}\theta)]$ & $1/[\theta(1{-}\theta)]$ & $n/[\theta(1{-}\theta)]$ \\
$N(\mu,\sigma^2)$ & $(x{-}\mu)/\sigma^2$ & $1/\sigma^2$ & $n/\sigma^2$ \\
\end{tabular}

\textbf{(b) Efficiency}: Exam gives $T$ and $\text{Var}(T)$. Compare to CRLB $= 1/(nI_1)$.

$\text{Var}(T) = $ CRLB $\Rightarrow$ efficient. \; $\text{Var}(T) > $ CRLB $\Rightarrow$ not efficient.

\textbf{(c) Asymp MLE}: $\hat\theta_n \sim_{\text{approx}} N(\theta,\; 1/(nI_1(\theta)))$. Just plug in $I_1$.

Poi: $N(\theta, \theta/n)$. Exp: $N(\theta, \theta^2/n)$. Ber: $N(\theta, \theta(1{-}\theta)/n)$.

\textbf{(d) CI}: Plug $\hat\theta$ for $\theta$: $\;\hat\theta \pm z_{\alpha/2}\sqrt{1/(n\,I_1(\hat\theta))}$.

Exp: $1/\bar X \pm z_{\alpha/2}\cdot(1/\bar X)/\sqrt{n}$. \; Poi: $\bar X \pm z_{\alpha/2}\sqrt{\bar X/n}$.

% ------------------------------------------------------------
\subsection{W12: CI Width Scaling}

CI width $\propto 1/\sqrt{n}$. To cut margin of error by factor $k$: $\boxed{n_{\text{new}} = k^2 \cdot n_{\text{old}}}$

Ex: margin $1/3$ as large $\Rightarrow n_{\text{new}} = 9 \cdot n_{\text{old}}$.

\textbf{Sample size for target margin $E$} (proportion): $n \ge z_{\alpha/2}^2\,\hat p(1{-}\hat p)/E^2$.

Conservative ($\hat p = 0.5$): $n \ge z_{\alpha/2}^2/(4E^2)$.

\textbf{Via Chebyshev}: $P(|\bar X - \mu|\le k\sigma) \ge 1-\delta$ $\;\Rightarrow\;$ $n \ge 1/(k^2\delta)$.

% ------------------------------------------------------------
\subsection{W13: Sufficiency (Two Methods)}

\textbf{Given}: distribution $f(x|\theta)$ and statistic $T$. \textbf{You need}: distribution of $T$ for Method B.

\textbf{Method A: Factorization} (default, faster):

Write joint $f(\mathbf{x};\theta) = \underbrace{[\text{no }\theta]}_{u(\mathbf{x})} \cdot \underbrace{[\theta\text{ only thru }T]}_{v(T,\theta)}$. Done.

Poi: $\frac{e^{-n\theta}\theta^{\sum x_i}}{\prod x_i!} = \frac{1}{\prod x_i!}\cdot e^{-n\theta}\theta^{\sum x_i}$. $T=\sum X_i$. $\checkmark$

Exp: $\theta^n e^{-\theta\sum x_i} = 1 \cdot \theta^n e^{-\theta\sum x_i}$. $T=\sum X_i$. $\checkmark$

$N(\mu,\sigma^2$ known): $\propto \exp\!\big(\frac{\mu\sum x_i}{\sigma^2} - \frac{n\mu^2}{2\sigma^2}\big)\cdot\exp\!\big(\frac{-\sum x_i^2}{2\sigma^2}\big)$. $T=\sum X_i$. $\checkmark$

\medskip

\textbf{Method B: Conditional} (when problem says ``using the definition''):

Compute $f(\mathbf{x}|T{=}t) = f(\mathbf{x};\theta)\,/\,P(T{=}t;\theta)$. All $\theta$ must cancel.

\textbf{Need to know $P(T{=}t)$}: Ber $\Rightarrow T\sim$ Bin$(n,\theta)$; Poi $\Rightarrow T\sim$ Poi$(n\theta)$.

Ber: $\frac{\theta^t(1-\theta)^{n-t}}{\binom{n}{t}\theta^t(1-\theta)^{n-t}} = \frac{1}{\binom{n}{t}}$. No $\theta$. $\checkmark$

Poi: $\frac{e^{-n\theta}\theta^t/\prod x_i!}{e^{-n\theta}(n\theta)^t/t!} = \frac{t!}{n^t\prod x_i!}$. No $\theta$. $\checkmark$

\end{multicols}
\end{document}
